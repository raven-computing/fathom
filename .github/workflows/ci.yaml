name: 'CI'
on:
  push:
    branches:
      - '**'
    tags-ignore: ['v*']

jobs:
  test:
    name: 'Test'
    uses: ./.github/workflows/test.yaml
    secrets: inherit

  build:
    name: 'Build'
    needs: test
    uses: ./.github/workflows/build.yaml
    secrets: inherit

